{
  "name": "metis-trail",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --watch",
    "build": "node scripts/build.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "vitest": "2.1.9"
  }
}
